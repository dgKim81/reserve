import TimeSpan from "./domainFramework/TimeSpan";

export default class ReservationService {
    getRoomReserveableTimeLine() {

    }

    reserveRoom(roomId: number, times: TimeSpan[]) {

    }

    getReservationStatus(reservationId: number) {
        
    }
}